#include <stdio.h>
#include <stdint.h>
int make_flags(int[] subnet1,int mask1,
            int[] subnet2,int mask2,uint32_t ip,uint_16t port)
{
    int false_lan = 0;
    int same = 0;
    int fullymask = mask1/8;
    for(int i=0;i<fullymask;i++){
        if()
    }
}